services:
  homepage:
    build:
      context: .
      dockerfile: Dockerfile
    image: nyatori-homepage:latest
    container_name: nyatori-homepage
    restart: unless-stopped
    ports:
      - "127.0.0.1:${NYATORI_PORT:-8080}:80"
